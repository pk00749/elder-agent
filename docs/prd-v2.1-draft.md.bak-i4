# 老友 PRD v2.1 增补（草案）

> 文档版本：v2.1 增补（草案）
> 文档状态：**草案**，待评审；不替换 v2.0，本文件为追加式增补
> 基础版本：[prd.md](../prd.md)（v2.0 定稿）
> 范围：仅细化 v2.0 中"未落到 UI"的描述；不动架构、数据模型、API 契约
> 凡本文档未提及的条款，全部沿用 v2.0

## 0. 修订说明

v2.0 已锁定架构与功能定义，但 UI 层仍偏粗。本草案在不动 §1–§10 的前提下，新增/扩展以下章节：

| 新增/扩展 | 章节 | 关联 v2.0 章节 |
|-----------|------|----------------|
| 新增 | §A. 全局 UI 规范 | §4 老年友好设计（硬约束保留并量化） |
| 新增 | §B. 启动与身份选择 | §2 用户与角色 |
| 新增 | §C. 登录与鉴权流程 | §3.3 跨端流程 + §7.4 |
| 扩展 | §D. 老人端各屏细化 | §3.1.1 / §3.1.2 / §3.1.3 |
| 扩展 | §E. 家属端各屏细化 | §3.2.1 / §3.2.2 / §3.2.3 |
| 扩展 | §F. 绑定流程细化 | §3.3 |
| 新增 | §G. 系统级 UI 行为 | §4 + §10 |
| 新增 | §H. 待澄清项 | §11 |

v2.0 中 §3.1.4 Agent 行为约束（硬规则 A/B/C/D）已完整，本文档**不复述、不修改**；服务端的 system prompt 与 tool 定义继续引用 §3.1.4。

## 1. 本次评审关键决策（已锁定）

| # | 决策点 | 选定方案 |
|---|--------|----------|
| 1 | 视觉气质 | 暖白底 `#FAF7F2` + 墨绿主色 `#4A7A4A`（次级强调暖橙 `#E07A3C`，警示深红 `#C44545`，主文字 `#1F1F1F`，次文字 `#5A5A5A`，分割线 `#DCD5C9`） |
| 2 | 老人账号创建 | 老人首启只选身份（无需手机号）；家属扫码/输入手机号绑定时，在老人设备上点同意→同时建 elder_profile + binding |
| 3 | AI 访谈保存反馈 | TTS 播报 Agent summary（≤ 60 字）+ 主屏「今日记录」图标亮起 + 短震 |
| 4 | 家属端「提醒」Tab 布局 | 一个 Tab 内两个分段「吃药 / 看医生」，各自分段顶部 + 号入口 |

---

## A. 全局 UI 规范

### A.1 色彩 Token

| Token | 值 | 用途 |
|-------|----|----|
| `color.bg.primary` | `#FAF7F2` | 屏幕底色（暖白） |
| `color.bg.card` | `#FFFFFF` | 卡片底色（与暖白轻微反差） |
| `color.bg.elevated` | `#FFF4E6` | 高亮卡片底（就医提醒、待办） |
| `color.text.primary` | `#1F1F1F` | 主文字 |
| `color.text.secondary` | `#5A5A5A` | 副文字、说明 |
| `color.text.disabled` | `#9A9A9A` | 禁用态 |
| `color.brand.primary` | `#4A7A4A` | 墨绿主色——主按钮、激活态、强调 |
| `color.brand.primary_pressed` | `#3C623C` | 主按钮按下 |
| `color.accent.warm` | `#E07A3C` | 暖橙——就医提醒、辅助强调 |
| `color.danger` | `#C44545` | 警示——错误、删除、未应答 |
| `color.divider` | `#DCD5C9` | 分割线 |
| `color.shadow` | `rgba(31,31,31,0.08)` | 卡片阴影 |

对比度核查：所有文字/底色组合需 ≥ 4.5:1（WCAG AA）。`#5A5A5A` on `#FAF7F2` ≈ 7.6:1；`#4A7A4A` on `#FAF7F2` ≈ 4.8:1，符合。

### A.2 字号 Token（sp）

| Token | 值 | 用途 |
|-------|----|----|
| `font.hero` | **40** | 老人主屏问候（"早上好"） |
| `font.title` | **32** | 屏幕大标题（"该吃药啦"、"今日记录"） |
| `font.body` | **24** | 正文（卡片副标题、表单标签） |
| `font.caption` | **20** | 副文字、说明、时间戳 |
| `font.button` | **32** | 所有按钮文字 |
| `font.timer` | **48** | 录音时长显示 |

约束：以上为默认值；老人端设置可放大（见 §D.7）。家属端沿用相同字号（不放大）。

### A.3 间距 Token（dp）

| Token | 值 | 用途 |
|-------|----|----|
| `space.screen.padding_x` | 24 | 屏幕左右内边距 |
| `space.section.gap` | 24 | 屏内主要元素垂直间距 |
| `space.card.padding` | 20 | 卡片内边距 |
| `space.button.height` | **≥ 96** | 所有按钮高（含主屏写日记大按钮 120dp） |
| `space.button.gap` | 16 | 按钮组内间距 |
| `space.row.height` | 72 | 列表行高（提醒项、日志项） |

### A.4 圆角与动效

| 项 | 值 | 说明 |
|----|----|----|
| 卡片圆角 | **8 dp** | 不使用 16+ 大圆角（与老年克制气质保持一致） |
| 按钮圆角 | **8 dp** | 同上 |
| 元素入场动效 | ≤ 300 ms | 仅 fade / 极轻 slide；不引入弹性/缩放 |
| 提醒卡出现 | 全屏淡入 250 ms + 系统级震动 + TTS 立即播 |
| 加载态 | 转圈组件禁用；用「正在保存…」文字 + 进度条 | 转圈对老人不直观 |

### A.5 声音与反馈

| 场景 | 反馈 |
|------|------|
| 按钮按下 | 极短"哒"音 + 触觉反馈（light impact） |
| 录音开始/结束 | 短"嘟"音 + 触感（medium） |
| 任务完成（保存、应答） | 上扬"叮"音 + 触感（heavy） |
| 错误/失败 | 双短"嘟-嘟" + 触感 |
| TTS 播报 | 默认开启；老人端设置可关 |
| TTS 服务 | **千问 TTS**（DashScope / CosyVoice），默认音色 **粤语男声**（见 §H.15 决议） |

音量默认跟随系统媒体音量；不在 App 内提供"静音 App"开关（避免老人误关导致听不见提醒）。

---

## B. 启动与身份选择

### B.1 启动序列

1. **Splash**（2 秒，不可跳过）
   - 居中 Logo（"老友"二字 + 一片叶子图形）
   - 暖白底 `#FAF7F2`
   - 不出现版本号/广告/引导轮播
2. Splash 结束后判断本地状态：
   - 本地已存 `role`（`elder` / `family`）→ 直接进入对应主屏
   - 本地无 `role` → 进入身份选择屏（§B.2）

### B.2 身份选择屏

| 元素 | 规格 |
|------|------|
| 屏幕标题 | "您好，请选择身份"（32sp，居中） |
| 卡片 A：「我是老人」 | 高度 200dp，宽度 = 屏宽 - 48dp 内边距；圆角 8dp；白底；左侧大号人形图标 96dp；右侧"我是老人"32sp + 副文字"记日常、听提醒"20sp；点击进入老人主屏（**不走登录**，见 §B.3） |
| 卡片 B：「我是家属」 | 同高度；右侧"我是家属"32sp + 副文字"配提醒、看日志"20sp；点击进入家属登录流程（§C） |
| 两卡间距 | 24dp |
| 底部小字 | "不确定选哪个？"24sp + TTS 按钮 ▶（播报两段说明） |

不在此屏注册账号；账号创建时机见 §B.3 / §F。

### B.3 身份切换

老人/家属身份**不可在 App 内直接切换**。切换的唯一路径：进入对应端「设置 → 退出登录」→ 清空本地 `role` → 回到身份选择屏。

理由：MVP 不做多账号，单设备单身份最简单。

---

## C. 登录与鉴权流程（仅家属走 SMS）

> v2.0 §7.4 描述 JWT，本节落到 UI 流程。

### C.1 适用对象

| 端 | 登录方式 |
|----|----------|
| 家属 | SMS 验证码登录（强制） |
| 老人 | **首启不登录**；绑定时才生成 `elder_profile`（§B.3、§F） |

### C.2 手机号输入屏

| 元素 | 规格 |
|------|------|
| 顶部 | "请输入手机号"32sp 标题 |
| 输入框 | 大号数字键盘（`inputType="phone"`），字号 28sp；占位文字"11 位手机号"24sp 灰 |
| 按钮 | "获取验证码"——输入合法后高亮（墨绿），否则灰禁用；按钮高 96dp |
| 隐私小字 | "仅用于登录，不会发送广告短信"20sp 灰色，居输入框下方 |

### C.3 验证码屏

| 元素 | 规格 |
|------|------|
| 顶部 | "验证码已发送至 138****8888"24sp（自动遮中间四位） |
| 验证码输入 | **6 格独立输入框**，每格 64×80dp，字号 36sp；首格聚焦；输完自动提交 |
| 倒计时 | "59 秒后重发"20sp 灰；可点时变"重新获取"墨绿 |
| 按钮 | "登录"——6 位填满后高亮可点；或回车键自动提交 |
| TTS | 进入即 TTS 播报"验证码已发送，请在手机上查收" |

### C.4 失败与重试

- 验证码错误：底部 Toast "验证码不对，再试一次"24sp 1.5 秒；同时清空输入让老人重新输
- 60 秒内同一手机号重复请求：`429 RATE_LIMITED`，前端按 `retry_after_seconds` 禁用按钮
- 5 次失败锁定 5 分钟（v2.0 §6.1）：弹"请 5 分钟后再试"全屏提示卡 + TTS 播报

### C.5 登录成功

写入本地：`role=family`、JWT（v2.0 §7.4）、`family_user.id`；进入家属端主屏（§E.1）。

---

## D. 老人端各屏细化

### D.1 主屏

布局自上而下：

```
┌─────────────────────────────────┐
│  区域 A：问候（高 200dp）        │
│   「早上好」  40sp 居中          │
│   「8 月 24 日 周一 09:12」20sp   │
│                                  │
├─────────────────────────────────┤
│  区域 B：今日提醒卡（高 120dp）  │
│   「下一项：吃阿莫西林 09:30」   │
│   或「今日暂无提醒」             │
│                                  │
├─────────────────────────────────┤
│  区域 C：写日记按钮（高 120dp）  │
│   「✎  按住说话写日记」          │
│   墨绿主色，文字 32sp 居中       │
└─────────────────────────────────┘
```

约束：
- 主屏元素 **恰好 3 个**（满足 v2.0 §4 第 2 条）；隐藏设置入口（连点中央 5 次）不计入
- 今日记录图标（决策 3）：保存日志后点亮——位置在 A 区右上角 32dp 图标位置；亮起 = 墨绿实心，未亮 = 灰色描边
- 写日记按钮按下：墨绿→`primary_pressed`，显示波形 + 时长（见 D.3）
- 设置入口：连点 A 区中央 5 次（每次间隔 ≤ 500ms）→ 进入 §D.7 隐藏设置；进入时震动 + TTS"打开设置"

边缘情况：
- 当前时间已有提醒触发中：主屏直接被全屏提醒卡覆盖（D.2 / D.5）
- 设备锁屏：提醒仍以系统通知形式弹出（见 §G.1）

### D.2 服药提醒全屏卡

| 区域 | 内容 |
|------|------|
| 顶部 | 系统状态栏 + 大铃铛图标 96dp（暖橙 `#E07A3C`） |
| 标题 | "该吃药啦" 32sp 居中 |
| 副标题 | "阿莫西林 · 1 粒" 24sp 居中（药名 + 剂量来自 reminder.payload） |
| 备注 | 有则显示，20sp 居中灰 |
| 主按钮 | "✓ 已吃"——高 120dp，墨绿底，32sp 白字 |
| 兜底 | 5 分钟无点 → 自动写入"未服药"ack；再 5 分钟再次响铃；30 分钟仍无应答 → 推送家属（v2.0 §11.2） |

行为：
- 弹出瞬间：震动 800ms + TTS"该吃阿莫西林了"
- 屏幕保持亮（`FLAG_KEEP_SCREEN_ON`）
- 点"已吃"：写 reminder_ack（`action=taken`）→ 播放上扬"叮" + 震动 → 卡片淡出 → 回到主屏
- 音量键/TTS 重复：右上角小图标"再听一遍"24dp，点击重播当前 TTS

**多药并发（§K.1a 决议）**：同一时刻触发 ≥ 2 条 reminder 时，**合并为一张卡**——主标题保持"该吃药啦"；副标题区按行纵列所有药名剂量（行高 48dp，字号 24sp，最多支持 4 种药；超出则拆第二张）；主按钮文案改为"✓ 全部已吃"——点击一次性 ack 所有；TTS 播报按顺序读全部药名。

**漏服回顾（§K.1c 决议）**：老人端**不展示**漏服统计（避免心理压力）；漏服 ack 写入后端，仅家属端可见。

**临时暂停（§K.1e 决议）**：老人端**不提供**暂停入口；暂停/恢复统一在家属端（§E.2 吃药项开关）。

**半粒视觉（§K.1f 决议）**：主图标按剂量切换：
- "1 粒" → 整药丸
- "半粒" → 切半药丸（完整药丸 + 半药丸并列）
- "1 勺" → 药瓶 + 勺
- "自定义" → 通用药丸 + 数字角标

### D.3 AI 访谈屏

布局：

```
┌─────────────────────────────────┐
│  进度条：「第 2 轮 / 共 8 轮」   │  ← 24sp 居中，灰
├─────────────────────────────────┤
│  Agent 回复区（高 200dp）        │
│   「今天都干啥了？」  32sp       │
│   ▶ TTS 播放中图标（脉动）       │
├─────────────────────────────────┤
│  老人本轮语音条（如有）          │
│   「……」（ASR 转写）  20sp 灰    │
├─────────────────────────────────┤
│  按住说话按钮（直径 160dp）      │
│   未按：灰底「按住说话」         │
│   按下：墨绿 + 波形条 + 时长     │
└─────────────────────────────────┘
```

逐轮状态机（按下 → 松开）：

1. **按下**（`onTouch DOWN`）：按钮变墨绿，开始录音（`MediaRecorder`，m4a）；开始计时；波形条启动
2. **松开**（`onTouch UP`）：停止录音 → POST `/v1/agent/asr`（音频已上传 COS）→ 拿 ASR 文字
3. **ASR 完成**：POST `/v1/agent/diary-session/:id/turn` 拿 Agent 回复（轮次未达上限）
4. **Agent 回复到达**：POST `/v1/agent/tts` 拿音频 → 立即播放 → 老人侧显示回复文字 + 播放脉动图标
5. **状态机推进**：进度条"第 N 轮 / 共 8 轮"刷新；按钮回到"按住说话"
6. **收尾触发**（满足 §3.1.4.C 任一）：跳到 D.4 总结屏

异常处理：
- ASR 失败：弹出"没听清，再说一次"大按钮（96dp 高）；老人按下后回到本轮重新录音，不消耗轮数
- Agent 30 秒超时：v2.0 §3.1.2 兜底——TTS 播"今天到这儿吧，明天继续" + 自动跳 D.4
- 老人中途退出（按返回）：保存已有 turns → status=`abandoned`，回到主屏

**方言 ASR（§K.2e 决议）**：服务端 ASR 走**千问 ASR**，**主识别粤语**（与 TTS 粤语男声统一调性）；普通话自动兜底识别。其他方言（四川话、上海话等）若 ASR 置信度 < 0.6，弹"听不太清，再说一次"走录音重试流程（不消耗轮数）。

**多人说话（§K.2d 决议）**：**不支持 VAD 切分**，整体录音 + 整体 ASR；Agent 收到长文本后由 LLM 自行归纳为老人视角的日记。家庭同堂场景提醒：可由老人事先告知"等家人走了再按"。

**录音技术规格（§L.3 决议 + 后台录音方案 D）**：
- API：`MediaRecorder`，输出 AAC/m4a（与 v2.0 §5.8 COS key 后缀一致）
- 采样率 / 编码：16 kHz / 单声道 / 64 kbps——够 ASR + 省流量
- 屏幕保持亮：`FLAG_KEEP_SCREEN_ON`（v2.0 §D.3 已要求）
- 单轮时长硬限：**60 秒**——到时震动 + TTS"时间到，松手吧"+ 自动停止
- 应用进入后台（`onStop()`）：立即停止 → 保存已录音频 → 弹"录音被打断，回到主屏"
- 系统来电打断：同上走 §D.3 中途退出流程（保存已有 turns → status=`abandoned`）
- 录音失败：自动重试一次，仍失败走 §D.3 ASR 失败兜底
- 录音中顶部状态条："正在录音… 00:23"（红色脉冲，字号 32sp）
- 麦克风权限拒绝（§G.1）：弹"需要麦克风权限才能写日记"+ 跳转系统设置

**为什么不上前台 Service（方案 D 决策依据）**：Android 14 `FOREGROUND_SERVICE_TYPE_MICROPHONE` 启动后必须保持 Activity 可见，否则杀服务——老人一切后台就失效；国内厂商 ROM 自启动 / 电池优化适配成本高；业务上单轮 < 60s 不需要长录音。

### D.4 访谈总结屏

进入条件：Agent 触发收尾（§3.1.4.C）。

布局：

```
┌─────────────────────────────────┐
│  标题：「今天记好了 ✓」  32sp    │
├─────────────────────────────────┤
│  日记正文卡                      │
│   「今天和老张下了盘棋，         │
│     我赢了，挺高兴。」          │
│   24sp 主文字，行高 1.6          │
├─────────────────────────────────┤
│  一句话摘要：                    │
│   「和老张下棋，赢了」  20sp 灰  │
├─────────────────────────────────┤
│  按钮组（垂直两枚，高 96dp）     │
│   「✓ 保存」  墨绿               │
│   「✎ 改一下」  描边墨绿         │
└─────────────────────────────────┘
```

交互：
- 默认 TTS 播 summary 一遍（决策 3）
- 点"保存"：写 `diary_entry` + finalize session → 主屏「今日记录」图标亮起 + 短震 + 上扬"叮" + 回主屏
- 点"改一下"：回到 §3.1.2 第 4 轮加一轮（最大再加 2 轮），再次进入收尾
- **不提供"删掉重录"入口（§K.2g 决议）**：误录只能走"改一下"加一轮补充，不允许丢弃已有 turns

### D.5 就医提醒全屏卡

结构基础同 D.2，差异：

| 区域 | 内容 |
|------|------|
| 顶部 | 系统状态栏 + 医院十字图标 96dp（暖橙 `#E07A3C`） |
| 标题 | "该去看医生啦" 32sp 居中 |
| 副标题 | "协和医院 · 心内科 · 上午 10:00" 24sp 居中 |
| 准备清单 | 家属端填写的备注，20sp 居中灰（§K.3e）——例："带医保卡、带上周验血报告" |
| 主按钮 | "✓ 知道了"——action=`ack` |
| 兜底 | 同 D.2：5/30 分钟节奏 |

**提前提醒（§K.3a 决议）**：**默认提前 1 小时**先弹一次"准备出门"预备卡（高度同 §D.2 但无主按钮，只有"知道了"+ TTS 播报）；到点再弹正式全屏卡。提前时间家属端可调（v2.0 §3.2.2 表单新增"提前提醒"字段，默认 60 分钟，可选 30/60/120）。

**多条合并（§K.3b 决议）**：同时段多条就医提醒**叠**——依次弹出全屏卡，前一张"知道了"才能进下一张；不合并。

**改约同步（§K.3c 决议）**：家属端把就医时间**往后调**时，老人侧主屏提醒卡自动刷新（拉到最新 payload）；**仅支持往后调**——往前调（立即/过去时间）后端直接拒，返回 422 `VALIDATION_ERROR`。

**复诊周期（§K.3d 决议）**：v2.0 §5.4 `reminder.payload.appointment.repeat` 字段**不支持 UI 录入**；每次就医提醒家属手建。即使同医院同科室每周复诊，也不自动续期。

**路线导航（§K.3f 决议）**：v2.2 实现。

**完成反馈（§K.3g 决议）**：老人点"知道了"→ 写 ack + 后端发推送 → 家属端通知栏收到"老人已确认 10:00 就医"；家属端日志 Tab / 提醒 Tab 同时可见。

### D.6 老人端「今日记录」图标点亮规则（决策 3）

```
未亮：灰色描边铃铛/笔图标
亮起条件：当日存在至少 1 条 status=finalized 的 diary_entry
熄灭条件：跨过本地零点；或新一天首次进入主屏
位置：主屏 A 区右上角 32dp
点亮反馈：图标颜色动画（300ms 渐入墨绿）+ 单次短震 100ms；不弹 Toast
```

**点击行为（§H.26 决议 + §K.2a 扩展）**：点开 → 进入**近期日志屏**：
- 顶部时间范围胶囊：`今天` / `近7天`（MVP 不做本月/全部）
- `今天` → 当日时间轴（每条：时间 + ASR 文字 + ▶ TTS 重播 + ▶ 听 Agent 回复语音）
- `近7天` → 按日期倒序分组，每组下显示该日所有 diary（每条：时间 + summary + ▶ 听完整）
- 列表为空时：分空态文案（今天："今天还没记"；近7天："近7天还没有日志"） + TTS 按钮
- 实现复用 §E.6 详情 UI，但**全部条目仅展示，不可编辑 / 不可删除 / 不可分享**

**删除权限（§K.2f 决议）**：老人端**不提供任何删除入口**——既不能删 diary_entry，也不能删 audio_segment；避免误操作 + 满足 v2.0 §11.3 日志归属原则。仅家属有查看权，无删除权（v2.0 §11.3）。

### D.7 老人端设置（隐藏入口）

进入：主屏 A 区中央连点 5 次（每次间隔 ≤ 500ms）。

布局：单列列表，每行高 72dp，24sp 文字，右向箭头。

| 行 | 标题 | 说明 | 行为 |
|----|------|------|------|
| 1 | 字体大小 | "大 / 特大"二选一切换 | 档位 sp 值（§H.16 决议）：默认 24/32 → **大 28/38** → **特大 32/44**（body/title） |
| 2 | 语音播报 | 开 / 关 | 关时所有 TTS 静音，仅震动 |
| 3 | 音量 | 跳转系统媒体音量设置 | `Intent ACTION_SOUND_SETTINGS` |
| 4 | 生成本机二维码 | — | 见 §F.3 家属扫码绑定 |
| 5 | 关于 | 版本号 / 服务条款 | 只读 |
| 6 | 退出登录 | — | 清空本地 `role` → 回身份选择屏（§B.3） |

退出登录对老人端的影响（§H.20 决议）：
- **未绑定** 时：直接清本地 + 回 §B.2
- **已绑定** 时：弹确认卡，提示语必须含——
  - "退出后将无法接收提醒"
  - **"再装此 App 需要家人重新绑定"**（重装不支持迁移，§H.20）
- 同意后：清本地 + 调 `/v1/bind/elder/:id`（仅 primary 可解绑，见 v2.0 §11.6）

---

## E. 家属端各屏细化

### E.1 家属主屏（Tab 容器）

进入后判断本地状态：

**未绑定态**（本地无 `binding` 记录）：

```
┌─────────────────────────────────┐
│  顶部："先添加一位老人"  32sp   │
├─────────────────────────────────┤
│  卡片 A：「输入手机号绑定」       │
│   输入框 + 「下一步」按钮        │
├─────────────────────────────────┤
│  卡片 B：「扫码绑定」            │
│   「打开相机扫一扫」  + 大图标   │
└─────────────────────────────────┘
```

流程分别见 §F.2 / §F.3。

**已绑定态**（存在至少 1 个 primary binding）：

```
┌─────────────────────────────────┐
│  顶部老人卡（高 96dp）           │
│   「妈妈 · 王秀英」 + 设备状态   │
├─────────────────────────────────┤
│  Tab Bar（高 64dp）              │
│   [ 提醒 ]  [ 日志 ]             │
├─────────────────────────────────┤
│  Tab 内容区                      │
└─────────────────────────────────┘
```

Tab Bar：底部固定，两个 Tab 等宽；当前 Tab 文字 + 下划线 4dp 墨绿。

### E.2 提醒 Tab（决策 4）

布局：单屏两个分段，每个分段顶部 + 号入口。

```
┌─────────────────────────────────┐
│  分段标题：「吃药」         [+] │
├─────────────────────────────────┤
│  · 阿莫西林  1 粒  09:30  [开关]│
│  · 降压药    半粒  19:00  [开关]│
│  · …                           │
├─────────────────────────────────┤
│  分段标题：「看医生」       [+] │
├─────────────────────────────────┤
│  · 协和医院 心内科  明天 10:00  │
│  · …                           │
└─────────────────────────────────┘
```

吃药项行（高 72dp）：
- 左：药名 24sp + 剂量 20sp 灰（垂直两行）
- 中：时间 24sp
- 右：开关 48dp（关=暂停；on=启用）；长按弹出菜单：编辑 / 删除

看医生项行（高 72dp）：
- 左：医院 + 科室 24sp
- 中：日期时间 24sp
- 右：无开关（一次性事件）；长按菜单：编辑 / 删除

分段 + 号：进入对应表单（§E.3 / §E.4）。

空状态：
- 整个 Tab 无任何提醒：居中显示"还没有提醒，点上面的 + 加一个"+ TTS 按钮 ▶

### E.3 服药提醒表单

单页表单（非分步骤），从上到下：

| 字段 | 控件 | 校验 |
|------|------|------|
| 药名 | 单行输入，28sp | 必填，1–20 字 |
| 剂量 | 单选卡片组（4 选 1 + 自定义）：`1 粒` / `半粒` / `1 勺` / `自定义`；选自定义显示数字输入框 | 必填 |
| 时间列表 | 列表，每行 `HH:mm` + 重复规则胶囊（每天 / 周一三五 / 自定义）；底部 `+ 加一个时间`；至少 1 条 | — |
| 备注 | 多行输入，限 100 字 | 选填 |

底部固定主按钮"保存"高 96dp；返回箭头在左上（系统行为，**不计入操作步骤**）。

### E.4 就医提醒表单

| 字段 | 控件 | 校验 |
|------|------|------|
| 医院 | 单行输入 | 必填 |
| 科室 | 单行输入 | 必填 |
| 日期时间 | 日期选择器 + 时分选择器（系统 Picker，但字号放大到 28sp） | 必填，必须未来时间 |
| 备注 | 多行输入，限 100 字 | 选填 |

底部主按钮"保存"高 96dp。

### E.5 日志 Tab

顶部筛选条（高 56dp）：

| 元素 | 控件 |
|------|------|
| 时间范围 | 横向胶囊组：`今天` / `本周` / `本月` / `全部`；单选 |

列表：按日期倒序，每行高 96dp：

```
┌─────────────────────────────────┐
│  8 月 24 日 周一                 │
│  「和老张下棋，赢了」20sp 灰     │
│  [▶ 听]                         │
└─────────────────────────────────┘
```

点行 → 进入 §E.6 详情。

空状态："还没有日志"+ TTS 按钮 ▶。

### E.6 日志详情

顶部：日期 + "本周第 N 篇"24sp。

主体：日记文字卡（24sp，行高 1.6）+ 每轮时间轴：

```
┌─────────────────────────────────┐
│  10:01  「和老张下了盘棋」       │
│         [▶ 听]                   │
│  10:03  「我赢了，挺高兴」       │
│         [▶ 听]                   │
└─────────────────────────────────┘
```

播放：调 `/v1/diary/:id` 拿预签 COS URL → MediaPlayer 流式播。

---

## F. 绑定流程细化

### F.1 总览

```
家属端                                老人端                       后端
│                                     │                            │
│ E.1 未绑定态选输入手机号或扫码       │                            │
│ ────────────────────────►            │                            │
│                                     │                            │
│                                     │   POST /v1/bind/elder      │
│                                     │   (family_id, phone or     │
│                                     │    device_token, name)     │
│                                     │                            │
│                                     │   ◄── 推送/轮询 ──►        │
│                                     │                            │
│                                     │   弹窗："XXX 想绑定，同意？" │
│                                     │                            │
│                                     │   同意 ──►                 │
│                                     │   创建 elder_profile       │
│                                     │   + binding(primary)       │
│                                     │                            │
│ ◄── 家属端收到 bind 成功 ──►        │                            │
│ 进入已绑定态                         │                            │
```

### F.2 方式 A：输入手机号绑定 — **MVP 不实现，推迟到 v2.2**（§H.23 决议）

MVP 仅保留 §F.3 扫码绑定一种方式。§E.1 未绑定态只保留「扫码绑定」卡片，「输入手机号」入口在 MVP 不渲染。

实现备忘（v2.2 落地时参考）：家属输入老人手机号 → 后端发送短链 → 老人点短链唤起 App → 进入 §F.4 确认；老人未装 App 时仅给"请先安装老友 App"+ 应用商店链接。

### F.3 方式 B：扫码绑定

家属侧流程：
1. 在 §E.1 点「扫码绑定」→ 调起相机（见 §G.1 权限）
2. 扫老人设备 §D.7 第 4 项生成的二维码（内容含 `device_token` + 临时 `bind_code`，5 分钟有效）

老人侧流程：
1. 进入 §D.7 第 4 项「生成本机二维码」→ 后端签发 `bind_code` → 在老人设备显示二维码（中心嵌 Logo）
2. 等待被扫（5 分钟内）；后台开始轮询 `/v1/bind/pending`（10 秒一次）
3. 收到扫到通知 → 弹 §F.4 确认卡

### F.4 老人侧绑定确认卡

| 元素 | 内容 |
|------|------|
| 标题 | "您的儿子/女儿 XXX 想绑定" 32sp（XXX=家属在 family_user 上填的称呼；缺则显示手机号尾四位） |
| 副文字 | "绑定后，XXX 可以给您设置提醒、看您记的日志" 20sp 灰 |
| 按钮 A | "同意"——墨绿，96dp 高 |
| 按钮 B | "拒绝"——描边灰，96dp 高 |

点击"同意"时（决策 2）：
1. 客户端调 `POST /v1/bind/confirm`，body`{ bind_code, elder_name }`
2. 后端在该事务内**同时**：
   - 创建 `elder_profile`（含 `name`、可选 `phone`、`device_token`、`timezone`）
   - 创建 `binding`（`role=primary`）
   - 返回 `elder_id`、`binding_id`
3. 老人端写入本地 `elder_id` + JWT；主屏更新绑定状态
4. 家属端收到推送/轮询"绑定成功"→ 进入 §E.1 已绑定态

点击"拒绝"：仅后端写 `bind_attempt` 失败记录，不创建 elder_profile；双方收到"已拒绝"提示。

**Primary vs Secondary（§H.25 决议）**：
- 同一 `elder_profile` 上，**首个完成老人同意的家属 = primary**（有解绑权，见 v2.0 §11.6）
- 后续家属邀请自动降级为 secondary；老人侧弹单卡"XXX 想添加为家庭成员"（与首位邀请文案差异：标题无"主"字、副文字改为"可帮您设提醒、看日志，但不能解除其他家庭成员"）
- secondary 邀请**仍需老人同意**（隐私敏感，不绕过）

### F.5 老人设备未绑定时的本地状态（§H.24 决议）

老人首启到绑定成功之间，存在以下本地状态：
- 本地有 `role=elder` + `device_token`，**无** `elder_id`
- 主屏"今日提醒卡"显示"等待家人配置提醒"
- 写日记按钮可用，录音 → ASR → 转写 → **暂存本地 Room 表 `pending_diary`**（不上传匿名 bucket），绑定成功后由 `flush_pending_diaries()` 批量 POST `/v1/diary`（带 `pending_id` 去重）

Room schema：
```
pending_diary(
  id           TEXT PRIMARY KEY,  -- 客户端 UUID
  created_at   INTEGER,           -- epoch ms
  audio_cos_key TEXT,             -- 已上传 COS 的临时音频 key（24h 过期）
  turns_json   TEXT,              -- Turn[] 序列化
  text         TEXT,
  summary      TEXT,
  status       TEXT               -- 'pending' | 'synced' | 'dropped'
)
```

未绑定时退出 App / 卸载 App：本地 Room 数据丢失，§D.7 第 6 项「退出登录」+「卸载 App」前应再次提示"未上传的日记会丢失"。

---

## G. 系统级 UI 行为

### G.1 权限申请时机

| 权限 | 申请时机 | 拒绝兜底 |
|------|----------|----------|
| 麦克风 `RECORD_AUDIO` | 首次进入 AI 访谈屏（§D.3） | 弹"需要麦克风权限才能写日记"+ 跳转系统设置；不进入访谈 |
| 相机 `CAMERA` | 家属点「扫码绑定」 | 弹"需要相机权限"+ 跳转系统设置；返回未绑定态 |
| 通知 `POST_NOTIFICATIONS`（Android 13+） | 首启身份选择完成后 5 秒（不在选择屏弹） | 提醒卡仍全屏弹出，但后台推送不到；不阻断 |
| 存储 | 不申请（MVP 不导出） | — |
| 位置 | 不申请（v2.0 §7.5） | — |

权限弹窗文案按 v2.0 §4 第 5 条"无手势操作"——只点一次"允许/不允许"，不教学。

### G.2 空状态

| 屏 | 空态文案 |
|----|----------|
| 提醒 Tab 整体 | "还没有提醒，点上面的 + 加一个" |
| 吃药分段 | "还没有吃药提醒" |
| 看医生分段 | "还没有就医提醒" |
| 日志 Tab | "还没有日志" |
| 日志详情空轮 | "本条日志没有语音" |
| 主屏提醒区（老人端） | "今日暂无提醒" |

所有空态配 TTS ▶ 按钮（24dp），点击播当前空态说明。

### G.3 错误 Toast

| 维度 | 值 |
|------|----|
| 字号 | 24sp |
| 时长 | 1.5 秒 |
| 位置 | 底部，距底 96dp |
| 颜色 | 白字 / `#C44545` 底 |
| TTS | 同步播报错误文案一次 |

常见文案（占位）：
- "网络好像断了，再试一次"
- "保存失败，再试一次"
- "服务器忙，过会儿再试"

### G.4 网络异常

顶部黄条（高 48dp，背景 `#FFF4E6`）出现于：
- 任意 `/v1/*` 请求 5xx 或网络断开
- 文案："网络好像断了，正在重试…"24sp；右侧"重试"按钮 96×48dp
- 恢复后自动隐藏 + 短震

### G.5 加载态

| 场景 | 表现 |
|------|------|
| 全屏加载 | 居中"加载中…" 24sp + 进度条（无菊花） |
| 局部加载 | 占位骨架屏（灰块 + 主色淡边） |

不出现任何 modal loading 遮罩——会遮挡老人看到的内容。

---

## H. 待澄清项（§11 增补）

| # | 项 | 已定方案（2026-08-24 评审） |
|---|----|---------------------------|
| 11.15 | TTS 默认音色与语速 | **千问 TTS**（DashScope / CosyVoice），默认音色 **粤语男声** |
| 11.16 | 老人端字体档位 sp 值 | 默认 24/32 → **大 28/38** → **特大 32/44**（body/title） |
| 11.17 | TTS 失败兜底 | 文字 + Toast + **系统铃声兜底**（绕过静音的闹钟/媒体通道） |
| 11.18 | 通知优先级 | 拆两条 TPush 通道：**服药 = 中优**（声音 + 通知栏，无全屏），**就医 = 高优**（绕过勿扰） |
| 11.19 | 老人端绑定二维码内容 | QR 仅含 `bind_code`；归属信息靠 UI 标题/Logo/倒计时传达 |
| 11.20 | 老人重装 App 账号恢复 | **不支持恢复**；§D.7 第 6 项退出登录需加提示"再装此 App 需要家人重新绑定" |
| 11.21 | 离线应答同步冲突 | **服务端时间优先**——保留最早收到那条，后到的写另一状态 |
| 11.22 | 家属端跨时区提醒显示 | 家属端显示跟随**设备系统时区**，不标注；MVP 不处理老人移动场景 |
| 11.23 | 短信绑定降级 H5 | **短信绑定整体不进 MVP**，仅做扫码绑定（§F.2 推迟 v2.2） |
| 11.24 | 老人未绑定前写日记暂存 | **本地 Room 暂存**（不上传匿名 bucket），绑定成功后 `flush_pending_diaries()` 批量同步 |
| 11.25 | 多家属同时邀请 | **时间最早成 primary**；后续降级 secondary，邀请仍需老人同意 |
| 11.26 | 「今日记录」图标点击 | 点开 → **当日日志时间轴**（TTS 重播每条） |

---

## I. 评审清单（Round 6 已闭环，2026-08-25）

§I 共 9 项经第六轮评审全部敲定，按来源归类汇总如下：

### I.1 本轮 6 项新增决议

| # | 项 | 决议 |
|---|----|------|
| I-1 | §A 色彩 token 是否追加品牌色 | **A. 不追加**——Logo 沿用 `#4A7A4A`（§A.4 不动） |
| I-2 | §D.1 主屏 3 元素是否含问候区 | **A. 含**——3 元素 = 问候 + 今日提醒卡 + 写日记按钮；隐藏设置入口不计 |
| I-3 | §D.2 5/30 分钟兜底节奏是否过密 | **A. 沿用**——v2.0 节奏不调；午睡场景列入 v2.2 复盘 |
| I-4 | §E.3 "自定义剂量"是否需要 | **C. 不做自定义剂量**——仅 `1粒 / 半粒 / 1勺` 三选一，特殊量走备注补充；**联动修改 §E.3 表单 / §D.2 半粒视觉 / §K.1f 图标映射** |
| I-5 | §E.5 日志筛选是否需要关键词搜索 | **A. 不做**——仅按日期浏览 |
| I-6 | §G.1 通知权限延后 5 秒后是否要在主屏给常驻小提示 | **A. 不补救**——5 秒后无提示，错过等下次提醒再说 |

### I.2 本轮复审 3 项（沿用既有决议）

| # | 项 | 决议 |
|---|----|------|
| I-7 | §D.7 字体档位 sp 值（§H.16） | **沿用 §H.16**：默认 24/32 → 大 28/38 → 特大 32/44，**无需复审** |
| I-8 | §F.2 / §F.3 绑定方式是否同时上 MVP | **§F.2 短信绑定已整体推 v2.2**（§H.23），仅保留扫码进 MVP，无需本节再议 |
| I-9 | §F.5 未绑定前写日记暂存 | **进 MVP**：本地 Room 暂存 + 绑定后 `flush_pending_diaries()` 同步（§H.24），无需再议 |

### I.3 联动修改清单（已落入草案正文）

- §E.3 剂量字段：4 选 1 + 自定义 → **3 选 1**（联动 §I-4.C）
- §D.2 半粒视觉段：删除"自定义"行（联动 §I-4.C）
- §K.1f 图标映射表：删除"自定义=通用药丸+数字角标"行（联动 §I-4.C）

---

> **草案状态**：§H 12 项（Round 1）+ §I 9 项（Round 6）已全部敲定并落入正文（见 §J.4 决议摘要）。下一步待用户确认是否合并入 prd.md。

---

## J. 本轮评审决议摘要（2026-08-24）

§H 的 12 项决议已落实到正文相应位置。摘要如下：

### J.1 已锁定产品决策

1. **TTS 供应商 + 音色**：千问 TTS，粤语男声（§A.5、§H.15）
2. **字体档位**：默认 / 大 28sp / 特大 32sp（body）（§D.7、§H.16）
3. **TTS 失败兜底**：系统铃声走闹钟/媒体通道（§H.17）
4. **通知优先级**：服药中优 / 就医高优，拆两条 TPush（§H.18）
5. **绑定二维码**：QR 内容仅 bind_code（§H.19）
6. **重装恢复**：不支持，退出登录加确认提示（§H.20、§D.7 第 6 项）
7. **冲突合并**：服务端时间优先（§H.21）
8. **跨时区**：家属端跟随设备系统时区（§H.22）
9. **短信绑定**：MVP 不做，§F.2 推 v2.2（§H.23）
10. **未绑定写日记**：本地 Room 暂存 + 绑定后批量同步（§H.24、§F.5）
11. **多家属**：时间最早成 primary，secondary 仍需老人同意（§H.25、§F.4）
12. **今日记录图标**：点开当日日志时间轴，TTS 重播（§H.26、§D.6）

### J.2 需要回写到 v2.0 主 PRD 的条目

合并到 prd.md 时，下列 v2.0 章节需同步修订：

| v2.0 章节 | 修订内容 |
|-----------|----------|
| §2 用户与角色 | 增补"老人首启不登录，绑定时建账号" |
| §3.3 跨端流程 | 删除"输入手机号 + 短信绑定"路径（§F.2 推 v2.2），仅保留扫码 |
| §4 老年友好设计 | 把字号/间距/动效硬约束改为引用 §A token 表 |
| §6 API 契约 | 新增 `POST /v1/bind/confirm`（§F.4）；`GET /v1/bind/pending`（§F.3 老人轮询） |
| §10.4 兼容性 | 增补"老人端 TTS 可出粤语"（与 §10.4 现有"仅中文普通话"协调） |
| §11 待澄清 | 11.15–11.26 全部填实并从"待定"挪到"已定" |

### J.3 AGENT.md 需要回写的条目

- §A.5 TTS 供应商改为千问 + 粤语男声
- §F.5 新增 Room schema `pending_diary` + `flush_pending_diaries()` 同步逻辑
- §F.4 新增 primary/secondary 邀请路径的服务端事务
- §3.2.1 / §3.2.2（reminder 创建）增加通知通道选择字段（`channel_priority: mid | high`）
- §3.2.2 就医提醒表单新增"提前提醒"字段（30/60/120 分钟，默认 60）
- §6 新增 `POST /v1/diary/flush-pending`（§F.5 批量同步端点）
- §5.4 reminder.payload.appointment `repeat` 字段 UI 不录入（§K.3d）

---

## K. 老人端三个功能细化决议（2026-08-24 第二轮评审）
### J.4 Round 6（§I）6 项决议摘要（2026-08-25）

| # | 决议 | 联动修改 |
|---|------|----------|
| I-1 | **不追加品牌色**——Logo 沿用 `#4A7A4A` | §A.4 不动 |
| I-2 | **主屏 3 元素 = 问候 + 今日提醒卡 + 写日记按钮**——隐藏设置入口不计 | §D.1 不动（已契合） |
| I-3 | **沿用 v2.0 5/30 分钟兜底节奏**——午睡场景暂接受，列入 v2.2 复盘 | §D.2 / §G.4 不动 |
| I-4 | **不做自定义剂量**——仅 `1粒 / 半粒 / 1勺` 三选一，特殊量走备注 | **联动**：§E.3 表单去掉"自定义"+ 数字输入框；§D.2 半粒视觉删"自定义"映射行；§K.1f 表删"自定义=通用药丸+数字角标"映射 |
| I-5 | **不做关键词搜索**——日志仅按日期浏览 | §E.5 不动（已契合） |
| I-6 | **通知权限延后 5 秒后不补救**——无常驻小提示 | §G.1 不动（已契合） |

**J.4.a 需要回写到 v2.0 主 PRD 的条目**

| v2.0 章节 | 修订内容 |
|-----------|----------|
| §3.2.1 服药提醒表单 | 删除"自定义剂量"控件和数字输入框；剂量字段限定 `1粒 / 半粒 / 1勺` 三选一；自定义语义改为备注承载 |
| §11 末尾 | 增补"老人端 3 元素 = 问候 / 提醒卡 / 写日记"硬约束 |

**J.4.b AGENT.md 需要回写的条目**

- §3.2.1 dosage 枚举从 `[PILL|HALF|SPOON|CUSTOM|VALUE]` 收紧为 `[PILL|HALF|SPOON]`，CUSTOM 走备注
- §5 reminder.payload.medication.dosage 同步：`{ type: 'pill' | 'half' | 'spoon', note?: string }`，删除 `custom_value: number`

---



### K.1 服药提醒

| # | 项 | 选定方案 |
|---|----|----------|
| 1a | 同时间多药 | 合并为一张卡，副标题纵列所有药名剂量（最多 4 种，超出拆第二张）；按钮"✓ 全部已吃" |
| 1b | 误触撤销 | **MVP 不实现** |
| 1c | 漏服回顾 | 老人端**不展示**漏服统计；家属端可见 |
| 1d | 时段标签 | **按时间排序**，不显示早/中/晚标签 |
| 1e | 临时暂停 | 老人端**不提供**暂停入口；统一在 §E.2 吃药项开关 |
| 1f | 半粒视觉 | 主图标按剂量切换：1粒=整药丸 / 半粒=切半药丸 / 1勺=药瓶+勺 / 自定义=通用药丸+数字角标 |

### K.2 AI 访谈写日志

| # | 项 | 选定方案 |
|---|----|----------|
| 2a | 历史回看 | 支持**今天 / 近7天**两个范围胶囊；今天=时间轴、近7天=按日分组 |
| 2b | 录音失败兜底（来电中断、存储满、Recorder 失败） | **v2.2** |
| 2c | 老人主动换话题 | **v2.2** |
| 2d | 多人说话 | **不支持** VAD；整体录音 + LLM 归纳 |
| 2e | 方言 ASR | **仅粤语主识别 + 普通话兜底**；其他方言置信度 < 0.6 走重试 |
| 2f | 日志删除 | 老人端**不提供任何删除入口**（满足 v2.0 §11.3 + 防误操作） |
| 2g | 删除重新录 | **不提供**；误录走"改一下"加轮 |

### K.3 就医提醒

| # | 项 | 选定方案 |
|---|----|----------|
| 3a | 提前提醒 | **默认提前 1 小时**先弹预备卡；时间家属端可调（30/60/120 分钟） |
| 3b | 同时段多条 | **叠**——依次弹出全屏卡，前一张"知道了"才能进下一张 |
| 3c | 改约同步 | 家属端把就医时间**往后调**；往前调后端 422 拒绝 |
| 3d | 复诊周期 | **不支持**——每次手设；v2.0 §5.4 `repeat` 字段 UI 不录入 |
| 3e | 准备清单 | **要**——家属端备注显示在副标题下方 |
| 3f | 路线导航 | **v2.2** |
| 3g | 完成反馈 | 老人"知道了"→ 写 ack + 推送家属端"老人已确认 10:00 就医" |

### K.4 三项决议触发的 PRD / AGENT.md 增量

- v2.0 §3.1.1 补「同时多药合并」规则
- v2.0 §3.1.3 补「提前提醒」+「准备清单」+「完成反馈」+「仅往后改」
- v2.0 §3.1.2 / §3.2.3 补「近7天回看」
- v2.0 §3.2.2 就医提醒表单新增「提前提醒」「备注」字段（备注以前就有，需明确必填/选填——默认选填，K.3e 决定）
- v2.0 §3.2.1 服药提醒表单「剂量」控件升级为 K.1f 图标映射
- v2.0 §11.6 补充"老人端日志仅查看，无删除权"

---

## L. 客户端架构与 ASR 决议（2026-08-24 第三轮评审）

### L.1 客户端栈

| 主题 | 选定方案 |
|------|----------|
| **UI 框架** | **Jetpack Compose**（大字号 / 动态改字号档更顺手） |
| **状态管理** | **ViewModel + StateFlow** |
| **DI** | **Hilt** |
| **网络层** | Retrofit + OkHttp + JWT 拦截器（自动续签 +401 重登录） |
| **本地存储** | Room（结构化：pending_diary / offline_ack）+ DataStore Preferences（JWT / role / 字号档等小数据） |
| **音频播放** | **ExoPlayer**（TTS 流式 + Agent 回复语音 + 老人自己的回放） |
| **音频录制** | MediaRecorder（m4a / 16 kHz / 单声道 / 64 kbps，见 §L.3） |
| **推送 SDK** | **TPush**（通道拆分见 §H.18） |
| **崩溃监控** | **Bugly**（国内稳定性优先于 Crashlytics） |

### L.2 后台录音策略（方案 D）

- **不**上 `FOREGROUND_SERVICE_TYPE_MICROPHONE` 前台服务（避免 Android 14 切后台即杀）
- AI 访谈屏必须保持前台可见，`FLAG_KEEP_SCREEN_ON`
- 单轮录音硬限 60 秒
- `onStop()` / 来电 / 弹窗 → 立即停止 + 保存已有 turns → 中途退出流程
- 应用进入后台不允许继续录音

### L.3 录音技术规格

- 16 kHz / 单声道 / 64 kbps / AAC m4a
- 失败自动重试 1 次，仍失败走 §D.3 ASR 失败兜底

### L.4 待澄清项（已在本轮敲定）

| # | 项 | 选定 |
|---|----|------|
| L1 | 厂商 ROM 白名单引导（自启动 / 电池优化） | **不做常驻引导**；ASR 连续失败 3 次后弹一次性提示 |
| L2 | 提醒卡触发通道 | **本地 AlarmManager 优先** + TPush 仅通知栏兜底 + 网络断开本地仍弹 |
| L3 | 录音采样率 / 比特率 | 16 kHz / 64 kbps |

### L.5 第三方 ASR 决议

- **千问 ASR**（DashScope / CosyVoice），与 TTS 同供应商（§H.15）
- 主识别**粤语**（与 TTS 粤语男声统一调性，§K.2e），普通话自动兜底
- 其他方言置信度 < 0.6 走录音重试流程

### L.6 模块化建议（落地参考）

```
app/
  elder/        — 老人端 UI + ViewModel
  family/       — 家属端 UI + ViewModel
  shared/       — 身份选择、登录、绑定（两端共享）
  core/
    ui/         — Compose 主题、字号档、Token
    data/       — Room / DataStore / Retrofit
    domain/     — Repository / UseCase
    audio/      — ExoPlayer 封装、MediaRecorder 封装
    push/       — TPush 通道路由
    crash/      — Bugly 初始化
```

### L.7 AGENT.md 需要回写的条目

- §3.2.1 reminder 表单提交时服务端同步注册本地 AlarmManager 指令（手机端收到 `set_alarm` 指令时落本地闹钟）
- §5.4 reminder.payload 增加 `local_alarm_token` 字段用于本地闹钟幂等

---

## M. 服务端架构决议（2026-08-25 第四轮评审）

### M1 agent-service 部署形态

**CVM 容器常驻**——单实例 Docker Compose 部署 DeepSeek Harness SDK + agent-service。

| 维度 | 选定 |
|------|----|
| 部署 | CVM 2核4G 单实例（MVP 1k 日活够用） |
| DSH 集成 | Python SDK 直接加载，长驻进程 |
| 长连接 | 支持（v2.0 §C 验证码轮询、§F.3 绑定轮询均可走 WebSocket） |
| 弹性 | 手动扩容；10k 日活以上再考虑 k8s |
| 成本（MVP） | ~¥300/月 |

不选 SCF：单轮响应延迟硬约束 8s（v2.0 §10.1），冷启动会推到边界；DSH 状态化服务不适合 serverless。

### M2 提醒下发机制（混合 push + 拉取）

**C 方案**——三层兜底：

1. **Push 即时**：家属 POST `/v1/reminders` → 服务端立刻 TPush 自定义消息 → 老人设备收到 → 立即 INCREMENTAL 同步 + 注册本地闹钟
2. **启动 / 网络恢复全量拉**：App 启动 / `onResume` / 网络恢复时 GET `/v1/reminders?from=today&days=14`，diff vs 本地闹钟表
3. **定时兜底**：每 6 小时 WorkManager periodic task → FULL_SYNC

设备侧同步状态机：

```
IDLE
 ├── on TPush custom message received → INCREMENTAL_SYNC
 ├── on app start / onResume / network recovered → FULL_SYNC
 └── every 6h (WorkManager periodic) → FULL_SYNC

FULL_SYNC:
  GET /v1/reminders?from=today&days=14
  diff vs 本地闹钟表:
    新增 → AlarmManager.setExactAndAllowWhileIdle(token)
    修改 → cancel old + set new
    删除 → cancel

INCREMENTAL_SYNC (from push payload):
  payload = { reminder_id, action: 'create|update|delete' }
  GET /v1/reminders/:id 详情 → 注册 / 更新 / 取消闹钟
```

依赖：§H.18 两条 TPush 通道（服药中优 / 就医高优）需支持自定义 payload；云端可控。

### M3 JWT 续签机制（双 token）

**B 方案**——access_token 1h + refresh_token 30d，OkHttp 拦截器无感续签。

| Token | 规格 |
|-------|------|
| **access_token** | HS256；payload = `{ user_id, role, elder_id?, exp=now+1h }`；Header `Authorization: Bearer <access>` |
| **refresh_token** | HS256；payload = `{ user_id, role, jti, exp=now+30d }`；一次性使用（每次 refresh 都轮换新 refresh） |
| **refresh 撤销列表** | CloudBase 简单集合 `revoked_refresh_jti`，登出 / 改密时写入 |
| **存储** | DataStore + EncryptedSharedPreferences 加密 |

端点：
- `POST /v1/auth/login`（v2.0 §6.1）→ 返回 `{access, refresh}`
- `POST /v1/auth/refresh`（新增）→ 用 refresh 换新 access + 新 refresh（轮换）
- `POST /v1/auth/logout`（新增）→ 撤销 refresh + 清本地

OkHttp 拦截器逻辑：
```
request → 检查 access 是否 < 5min 过期
  是 → 用 refresh 换新 access（并发锁避免重复刷新）
  否 → 直接发
response 401 → refresh 再换一次；换失败则跳登录
```

风险点：
- refresh 重放：用 jti 单次有效 + 设备指纹绑定
- access / refresh 同时过期（30d 不使用）→ 走 SMS 重登录

### M4 AGENT.md 需要回写的条目

- §8.2 agent-service 部署改为 CVM 容器常驻 + Docker Compose
- §6.1 新增 `POST /v1/auth/refresh`、`POST /v1/auth/logout`
- §5.x 新增 `revoked_refresh_jti` CloudBase 集合 schema
- §6.1 新增 reminder 推送指令 `set_alarm / cancel_alarm` 走 TPush 自定义消息

---

## N. 工程风险预案决议（2026-08-25 第五轮评审）

### N1 绑定通知实时性

**B 方案**——复用 M2 推送通道机制，TPush 自定义消息路由。

| 项 | 规格 |
|----|----|
| 推送目标 alias | `elder_id`（M2 已注册） |
| payload 类型 | `reminder` / `bind_request`（服务端按 alias 路由） |
| 实时性 | < 3s |
| 老人端接收 | TPush SDK 回调 → 拉 `/v1/bind/pending/:code` 详情 → 弹 §F.4 确认卡 |

不选 WS（心跳耗电、Android 后台断线率高）或 10s 轮询（实时性差）。

### N2 音频上传中断

**A 方案**——单次上传 + 失败重试 3 次（指数退避 1s/3s/9s）。

每轮上传流程：
1. POST `/v1/agent/diary-session/:id/upload-url` 拿预签 COS URL（5 分钟有效）
2. PUT 上传 m4a（超时 30s）
3. 失败 → 指数退避重试最多 3 次
4. 仍失败 → 弹"上传失败，是否重试？"（已成功的 turn 保留不丢）
5. 成功 → 调 `/turn` 端点拿 Agent 回复

切后台特殊处理：§L.2 录音中切后台 → 立即停止 + 中途退出；已上传 turn 保留 COS tmp 目录（24h 生命周期）；重新进入访谈屏 → 提示是否继续未完成 session。

### N3 老人端 diary 同步幂等

**A 方案**——客户端 UUID（`pending_diary.id`）作为 `Idempotency-Key` header。

```
flush_pending_diaries():
  for pending WHERE status='pending':
    POST /v1/diary
      Header: Idempotency-Key: <pending.id>
      Body: { elder_id, session_id?, text, summary, audio_segments[] }
    200 → 拿到 diary_id → status='synced'，存 server_diary_id
    409 → 服务端已处理同 key → 返回旧 diary_id → status='synced'
    5xx / 网络错 → status 保持 pending，下次 flush 重试
```

服务端实现要点：
- CloudBase `idempotency_keys` 集合（key + elder_id 联合索引）
- diary_entry 表加 `idempotency_key` 字段
- 写入新 entry 与 idempotency_key 同事务，失败回滚
- 幂等键永久保留（空间开销极小）

### N4 JWT 过期边界

**B 方案**——OkHttp Authenticator + Interceptor 双保险 + 单飞锁。

| 层 | 职责 |
|----|------|
| **Interceptor** | 主动检测 access 即将过期（`exp - now < 5min`）→ 提前 refresh |
| **Authenticator** | 401 兜底 → refresh 后重试原请求 |
| **单飞锁** | `Mutex` 串行化 refresh；30s 去抖避免重复刷新 |
| **死循环防护** | `responseCount(response) >= 2` 直接返回 null |

OkHttp 客户端配置：
- `connectTimeout 10s / readTimeout 30s`（agent 单轮响应可达 8s 留余量）
- 异常链：refresh 失败 → 清本地 + 跳 SMS 重登录

### N5 AGENT.md 需要回写的条目

- §6.1 新增 `POST /v1/bind/pending/:code`（N1 老人端拉详情）
- §6.1 新增 `POST /v1/agent/diary-session/:id/upload-url`（N2 预签 COS URL）
- §6.1 新增 `POST /v1/diary` 带 `Idempotency-Key` header 支持（N3）
- §5.x 新增 CloudBase `idempotency_keys` 集合 schema
- §5.x 新增 `diary_entry.idempotency_key` 字段
- §6.1 端点清单补充 401 处理约定（N4）
- §3.x 服务端 SDK 集成 OkHttp 客户端代码片段（示例）
